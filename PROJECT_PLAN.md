# 智能旅行规划 Agent — 项目总体规划

> 版本：v2.12（规划文档自身版本；产品里程碑版本见第 8 节）
> 创建日期：2026-09-06
> 维护规则：本文档为项目"宪法"，只做追加和版本化修订，不做静默修改
> 配套文档：PRD（待定稿）、docs/SESSION_LOG.md（会话记忆锚点，待建）

---

## 0. 这个项目是什么（大白话版）

你要做的是一个**会帮你做旅行攻略的 AI 助手**。

你只需要说一句："十一想去成都玩 3 天，预算 3000，带 5 岁小孩，不想太赶。"
它就能给你一份完整行程：每天去哪、每个景点怎么走（真实地图路线）、当天天气、每一笔钱花在哪，最后导出成一份能打印分享的 Word 行程单，还能把你的历史行程存起来随时翻看。

类比：它就像一个 24 小时在线、不收咨询费的定制游规划师——你说需求，它出方案，地图和天气数据都是查的真数据，不是 AI 编的。

---

## 1. 分层架构设计（这个系统长什么样）

整个系统分 6 层，像一家餐厅：

| 层 | 类比 | 干什么 | 本项目对应 |
|---|---|---|---|
| 前端层 | 餐厅大堂 | 你看得见、点得着的界面 | 网页（对话窗口 + 行程展示 + 地图） |
| 后端层 | 后厨 | 接单、调度、真正干活的 | Node.js 服务，管流程和存取 |
| AI 编排层 | 主厨 | 决定"这道菜怎么做" | 大模型调用 + 行程生成的提示词编排 |
| 外部服务层 | 食材供应商 | 提供真材料 | 地图 API、天气 API、大模型 API |
| 数据层 | 仓库 | 存东西 | SQLite 数据库（用户、行程、对话记录） |
| 导出层 | 打包外卖 | 把成品带走 | 生成 Word 行程单 |

### 一次完整请求的流动（产品闭环）

```
你在网页说需求 → 后端接住 → AI 编排层组装"提问"发给大模型
→ 大模型给出行程草稿 → 后端拿去地图/天气 API 核实补全（防止 AI 编造）
→ 拆预算 → 存入数据库 → 返回网页渲染展示 → 可导出 Word / 存入历史
```

关键设计原则：**AI 只负责"想"，地图和天气数据必须来自真实接口核实**。
这是旅行规划类 AI 产品最容易翻车的地方——大模型会一本正经地编造不存在的景点、错误的距离和营业时间。所以"事实校验"是独立的一环，不靠 AI 自觉。

---

## 2. 完整工作清单（阶段 → 产出 → 验收标准）

共 9 个阶段。每个阶段完成 = 产出物落盘 + 验收标准逐条通过 + Git 存档验证成功。

### 阶段 0：需求对靶与定位（当前阶段）
- **目标**：定清楚"给谁用、什么形态、哪些功能必做"。
- **产出**：需求文档（PRD v1.0），含功能清单（P0 必做 / P1 应做 / P2 可选）、用户场景、页面清单。
- **验收标准**：
  - [ ] 你能用自己的话说出"这个产品给谁用、解决什么问题"
  - [ ] P0 功能清单 ≤ 8 项（首版功能宁少勿多）
  - [ ] 你对 4 个对靶问题（见第 7 节）做出选择

### 阶段 1：产品原型（不写代码，先画样子）
- **目标**：把页面长什么样、点什么出什么提前定好，避免开发完才发现"不是我想要的"。
- **产出**：原型文档（每页一张线框图 + 交互说明），存 `docs/prototype/`。
- **验收标准**：
  - [ ] 3 个核心页面有原型：对话/输入页、行程结果页（地图主视图 + 一键导出）、历史记录页（含页内对话回放）
  - [ ] 每页标注"用户操作 → 系统反馈"
  - [ ] 你看过并确认"这就是我要的样子"

### 阶段 2：数据准备（办"供应商账号"）
- **目标**：注册并调通 3 类外部服务，拿到各自的"钥匙"（API Key）。
- **产出**：`docs/env-setup.md`（账号清单、Key 存放位置、调通记录）。
- **验收标准**：
  - [ ] 大模型 API：一条测试命令能返回 AI 回复
  - [ ] 地图 API：能查到"成都宽窄巷子"的位置和到"锦里"的路线
  - [ ] 天气 API：能查到成都未来 3 天天气
  - [ ] 所有 Key 存在本地 `.env` 文件（不上传 Git，有 .gitignore 挡着）

### 阶段 3：AI 核心模块（逐个开发、逐个验收）
- **目标**：把"规划师的大脑"拆成独立模块，一个一个做，做完一个验一个。
- **产出**：每个模块 = 代码 + 单独测试脚本 + 模块说明（写进 `docs/modules/`）。

| 编号 | 模块 | 优先级 | 说明 |
|---|---|---|---|
| M1 | 需求澄清对话 | P0 | 多轮追问补齐信息：日期/人数/预算/偏好/节奏。信息不全不硬编 |
| M2 | LLM 行程生成 | P0 | 主链路：输入结构化需求 → 输出每日行程草稿（JSON 格式） |
| M3 | 事实校验与地图补全 | P0 | 用地图 API 核实每个景点真实存在、补真实坐标/路线/耗时 |
| M4 | 天气补全 | P0 | 按行程日期查天气，遇雨天自动给室内替代建议 |
| M5 | 预算拆分 | P1 | 把总预算按交通/住宿/门票/餐饮拆分，超支时给调整方案 |
| M6 | 历史存储 | P0 | 行程 + 对话会话都存数据库；两个入口：「我的行程」列表、「对话记录」列表，均可重新打开继续查看/继续对话 |
| M7 | 文档导出 | P1 | 生成 Word 行程单（含每日安排表格） |
| M8 | 行程合理性校验 | P1 | 规则检查：同一天行程地理上是否顺路、时间是否冲突、是否违反常识（如凌晨逛景点） |

- **验收标准（每模块通用 + 举例）**：
  - [ ] 每个模块有独立测试脚本，跑通即绿
  - [ ] M2 示例：给 5 组标准需求，行程天数与要求一致、每日安排非空
  - [ ] M3 示例：行程里提到的景点 100% 能在地图 API 检索到（检索不到的会被替换或标注）
  - [ ] M1 示例：故意只说"去成都玩"，AI 会追问而不是硬编行程

### 阶段 4：后端开发
- **目标**：把各模块串成完整服务，前端能通过接口调用。
- **产出**：后端服务代码 + 接口文档（`docs/api.md`）。
- **验收标准**：
  - [ ] 核心接口 6 个以上：创建规划任务、对话澄清、获取结果、历史列表、历史详情、导出 Word
  - [ ] 用接口工具（如 curl / Postman）走通"提交需求 → 拿到完整行程"全流程
  - [ ] 异常有兜底：AI 超时/接口失败时返回友好错误，不白屏

### 阶段 5：前端应用开发
- **目标**：把原型做成真实网页，接上后端。
- **产出**：前端代码（对话流式输出、地图行程主视图 + 列表视图切换、历史行程/对话记录页（含页内对话回放视图））。
- **验收标准**：
  - [ ] 核心流程在浏览器里可完整走通：输入需求 → 看到生成过程 → 看到行程 → 导出 → 历史可查
  - [ ] 行程默认以地图视图展示：地点按顺序连线成路径，点标记弹出时间/地点/活动/花费详情；可切列表视图
  - [ ] 历史对话可回看：会话列表点开即在历史记录页内展开对话回放，可继续对话（AI 带着之前的上下文）；不设独立回放页
  - [ ] 表格行程单通过行程结果页「导出 Word」按钮直接下载（.docx），无独立导出页
  - [ ] 手机浏览器打开不乱版（响应式）
  - [ ] 与原型比对，主要交互一致

### 阶段 6：测试
- **目标**：模拟真实用户搞破坏，确保不轻易崩。
- **产出**：测试用例清单 + 测试报告（`docs/test-report.md`）。
- **验收标准**：
  - [ ] 10 组以上真实场景需求（亲子/穷游/商务/跨城等）全部生成成功
  - [ ] 恶意输入（空输入、超长文本、乱码）不导致服务崩溃
  - [ ] 预算计算抽查 5 组，金额加总与总预算吻合

### 阶段 7：部署上线
- **目标**：让任何人在浏览器输入网址就能用。
- **产出**：在线访问地址 + 部署文档（`docs/deploy.md`）。
- **验收标准**：
  - [ ] 换一台手机/电脑访问网址，完整流程可用
  - [ ] 服务重启后数据不丢（历史行程还在）
  - [ ] 出问题能一键回退到上一个正常版本

### 阶段 8：迭代维护
- **目标**：按真实使用反馈修问题、加功能。
- **产出**：问题记录清单，按周/按批修复。
- **验收标准**：每批修复有 Git 提交记录可追溯。

### 建议节奏（供参考，不是硬指标）
阶段 0-1 一次对话内完成 → 阶段 2 一次对话 → 阶段 3 分 3-4 次对话（每 2-3 个模块一次）→ 阶段 4-5 各 1-2 次 → 阶段 6-7 各 1 次。共约 10 次工作对话。

---

## 3. 代码存储与版本管理方案（存档与读档系统）

### 是什么（三句话讲完）
- **Git**：代码的"游戏存档系统"。每完成一段就存一个档（叫 commit），带编号和说明，随时可读回任何一个旧档。
- **GitHub**：把存档同步到云端的"备份盘"。电脑坏了档也不丢。国内访问偶尔抽风，备选 **Gitee**（国内的同类备份盘，界面几乎一样）。
- **回退**：改坏了就读回旧档，任何版本都能找回——这是敢大胆改代码的底气。

### 存档纪律（开发全程强制执行）
1. **触发时机**：每完成一个阶段/模块，或每修复一个 bug，立即存档；不攒。
2. **存档说明**：每条 commit 必须写清楚"做了什么"（例：`feat: M3 地图校验模块-POI真实性与坐标补全`）。
3. **里程碑打标签**：阶段完成时额外打 tag（例：`v0.1-原型确认`、`v0.5-核心模块完成`），像书签一样好找。
4. **验证后才报完成**（你点名的红线）：每次存储，我会依次执行——
   - 存本地档 → 查档记录确认"这条真的存在"
   - 推云端 → 查云端仓库确认"云端真的收到"
   - 两步都验证通过，才向你报告"存储完成"，并附上验证结果。

### 你需要做的一次性准备（到阶段 2 前后）
- 注册一个 GitHub 账号（如果还没有）。到时候我会一步步带你，全程只需点击和输入邮箱密码，不涉及任何技术操作。
- 电脑上 Git 装好即可（我来检查和处理）。

---

## 4. 对话记忆管理协议（防止聊着聊着"失忆"）

### 为什么需要
AI 助手的一次对话窗口有容量上限（类比：一张桌子的大小）。把所有历史消息原样堆在桌上，桌子会满，重要的会被挤出去。解决办法：定期把桌上东西整理成一份"备忘卡"，旧的原始聊天收起来。

### 本项目执行规则
1. **触发阈值**（两个先到先触发）：
   - 每完成 4-6 轮对话；
   - 每完成一个模块/阶段。
2. **摘要模板**（固定结构，替换老旧对话进入上下文）：
   ```
   【项目状态】当前阶段 / 完成度
   【已定决策】技术选型、产品口径（只列结论，不列讨论过程）
   【当前任务】正在做什么、做到哪一步
   【待办清单】下一步动作
   【遗留问题】已知 bug / 未验证项
   ```
3. **保留**：业务决策、方案选择结论、验收结果、bug 根因与修法、你的明确要求。
4. **丢弃**：闲聊寒暄、重复报错刷屏、中间调试输出、已被新决策推翻的旧讨论。
5. **跨对话锚点**：每次摘要同步落盘到 `docs/SESSION_LOG.md`（add-only 追加）。即使开全新对话，读这份文件即可无缝接上。
6. **产品侧记忆**（Agent 本身的记忆，属于 M1 模块功能）：Agent 在一次行程规划会话内记住你的偏好（人数/预算/节奏），多轮追问不重复问同一问题；会话结束即清，不跨用户保留。

---

## 5. 技术选型（我来定的部分，附理由和反悔方式）

以下属于技术路线选择，按分工我直接定，每项都留了"反悔路径"（换成本几乎为零的阶段都标注了）：

| 项 | 选择 | 一句话理由 | 反悔路径 |
|---|---|---|---|
| 前端 | Vue 3 + Vite + Pinia | 你拍板的主流方案替换（2026-09-06）；国内生态最热，中文资料最多 | 已定，随平台开发走 |
| 后端 | Python (FastAPI) + LangChain + **LangGraph** | 你拍板的主流方案替换（2026-09-06）；M1/M2 已用 LangChain 重写并测试通过，接口行为与 Node 版一致。**LangGraph 为 2026-09-06 对靶新增**：AI 规划流程编排为多节点状态图（理解→检索→排线→自检→输出）+ 流式输出（打字机效果） | 已定，随平台开发走；LangGraph 检索节点做成可替换组件，未来可无痛接 Neo4j |
| 数据库 | SQLite + SQLAlchemy 2.0 | 单文件数据库，零安装零配置；SQLAlchemy 是 Python 生态数据库操作的事实标准 | 换 PostgreSQL / MySQL 只改连接配置，成本中（MySQL 为参考清单项，已预留） |
| 缓存/会话 | JWT + cachetools（本机），Redis（上云后） | 本机无 Docker 跑不了 Redis；cachetools 进程内缓存顶上，接口已预留，上云无缝切换 | 上云部署时切 Redis，成本低 |
| 大模型 | 智谱 GLM-4-Flash（已调通） | 免费额度，LangChain 经 OpenAI 兼容协议接入 | 随时可换，LangChain 下只需改 base_url 和钥匙 |
| 地图+天气 | 高德开放平台（已调通） | POI/路线/天气一把抓，免费配额 | 阶段 2 内可换 |
| 文档导出 | Word (.docx) | 方便打印、转发、二次编辑 | 阶段 3 M7 内可加 PDF，成本低 |
| 接口测试 | pytest | Python 生态事实标准；M1/M2 测试已迁移，5/5 通过 | 已定 |
| 部署 | 先本机运行，后续可选公网 | 已拍板"先在自己电脑跑"，零成本零运维 | 想给朋友用时再补公网部署 |

**安全红线（全程自动执行）**：所有 API Key、密码只存本地 `.env` 文件，该文件被 .gitignore 规则挡住，永远不会被存档上传。

---

## 6. 产品闭环一览（做完后你能体验到什么）

```
打开网页 → 对话框说需求 → AI 追问补齐细节 → 生成中（能看到进度）
→ 地图行程：地点按顺序连线成完整路径，点标记看时间/地点/活动/花费；可切列表视图
→ 不满意？说"第二天太赶了" → AI 调整重出
→ 满意 → 一键存入"我的行程"（表格行程单仅作为 Word 下载导出选项保留）
→ 下次打开 → 「我的行程」翻旧行程继续用；「对话记录」回看任何一次会话并可接着聊
```

---

## 7. 需要你拍板的事（对靶清单）

以下 4 件事决定产品方向或涉及花钱，必须你来选（已附推荐和理由）：

1. **产品定位（给谁用）** — 推荐：A
2. **产品形态** — 推荐：A
3. **大模型（花钱项）** — 推荐：A 起步
4. **地图/天气数据源** — 推荐：A

（选项详情见对话中的选择题，选定后回填至此并升级为 v1.0 定稿）

---

## 8. 版本命名与里程碑（每个版本的小名）

规则：每达成一个里程碑，存档代码时打一个 tag（相当于书签，`git tag -n` 随时可查全部版本名）。版本号只涨不降，名字对应"这个版本你能看到什么新能力"。

| 版本 | 小名 | 这个版本你能看到什么 | 状态 |
|---|---|---|---|
| v0.1 | 开工图纸 | 规划定稿，确定做什么、怎么做 | ✅ 已完成（2026-09-06，tag 已打） |
| v0.2 | 样板间 | 3 个核心页面的样子确认（产品原型） | ✅ 已完成（2026-09-06，tag 已打） |
| v0.3 | 水电接通 | 两把钥匙全部调通：智谱（AI）+ 高德（地图/天气一把两用） | ✅ 已完成（2026-09-06，tag 已打） |
| v0.4 | 会聊天的助手 | 说一句话需求，AI 会追问补齐并给出行程草稿 | ✅ 已完成（2026-09-06，tag 已打；技术栈替换版另见 tag v0.4-fastapi） |
| v0.5 | 数据底座 | 高德 POI 批量爬取景点入库（首批 5 城）+ 数据库建库 + 用户登录注册 | ✅ 已完成（2026-09-06：8 表建库 + 5 城 530 条真实 POI + 注册/登录/我 三接口；6/6 测试通过，tag v0.5-data） |
| v0.6 | 有门有脸 | 首页（轮播+推荐+猜你喜欢）+ 旅游景点板块（搜索筛选详情收藏）+ 个人中心 + 管理后台 | ✅ 已完成（2026-09-06：21 项测试 + 真数据冒烟 PASS） |
| v0.7 | 图上谈兵 | 线路规划助手：整合 M1/M2 对话 + M3 地图核实 + M4 天气 + 地图标点连线 + 行程单页内直接展示 + 多方案保存切换对比 + 合理性校验；AI 规划升级 **LangGraph 多节点编排 + 流式输出**（2026-09-06 对靶新增；Word 导出已按 2026-09-06 变更移除） | ✅ 已完成（2026-09-08：四节点图 + SSE 流式 + 助手页 + **静态地图**（后端代理，key 不外泄）+ 天气 adcode 精确化；可拖动 JS 地图留待后续，需另申请 Web端 key） |
| v0.8 | 游记笔墨 | 旅游攻略板块（列表/详情/编写发布/关联景点） | ✅ 已完成（2026-09-08：列表/详情/编写三页 + 图集 + 草稿 + 检索；6 项测试 + 22 项真库冒烟 PASS） |
| v0.9 | 各就各位 | 个人中心（收藏/我的攻略/信息）+ 管理员后台（8 模块） | ✅ 已完成（2026-09-06 随 v0.6 提前交付） |
| v1.0 | 整装正式版 | 全链路测试打磨，日常可用 | 🔄 冲刺中（2026-09-08：全链路走查 51 项 + 边界测试 5 项全过 + README 补齐；待打 tag v1.0） |

（2026-09-06 版本序列重组：原 v0.5"眼见为实"、v0.6"有账本有记忆"、v0.7"能带走的行程单"、v0.8"靠谱检查员"、v0.9"前后打通"、v1.1"安家落户"的内容分别并入上表新序列——地图核实+天气并入 v0.7，Word 导出与合理性校验随 v0.7 一并实现，测试打磨并入 v1.0）

---

## 9. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-06 | v0.9 | 初稿：完整规划草案，待对靶定稿 |
| 2026-09-06 | v1.0 | 对靶定稿：定位=自用+身边人；形态=本机网页；模型=智谱 GLM-4-Flash；地图天气=高德；阶段 7 调整为本地运行优先 |
| 2026-09-06 | v1.1 | 新增第 8 节"版本命名与里程碑"（11 个版本小名）；v0.1 tag 已打 |
| 2026-09-06 | v1.2 | 需求变更（用户提出）：① 新增历史对话列表与回放续聊（M6 扩展）；② 行程展示改为地图主视图、点标记看详情、可切列表视图；③ 表格行程单降级为 Word 导出选项。阶段 5 验收标准与产品闭环同步更新，原型重做为 5 页 |
| 2026-09-06 | v1.3 | 需求变更（用户提出）：移除独立对话回放页。查看历史对话统一收进历史记录页（点会话卡「继续对话」在页内展开回放视图，可继续聊、可一键重新生成行程）。阶段 5 产出与验收同步修改；原型由 5 页改回 4 页，指向旧页④的全部页码引用已排查清理 |
| 2026-09-06 | v1.4 | 需求变更（用户提出）：移除独立行程单导出页。导出动作并入行程结果页「导出 Word」按钮（点击直接下载表格版 .docx，无中间页）。原型由 4 页收敛为 3 页；阶段 1 验收、阶段 5 产出/验收、版本表 v0.2 描述同步修改，引用已排查 |
| 2026-09-06 | v1.5 | 原型定稿（用户确认），打 v0.2「样板间」tag；阶段 2「水电接通」启动，建立 docs/env-setup.md 操作手册（2 把钥匙 = 智谱 + 高德，覆盖 AI/地图/天气 3 类服务） |
| 2026-09-06 | v1.6 | 阶段 2 完成：智谱 + 高德 4 项测试全绿（AI 回话/POI/路线/天气），证据存 env-setup.md；钥匙入 .env 并验证被 Git 规则挡住；打 v0.3「水电接通」tag。下一步阶段 3-AI 核心模块（从 M1 需求澄清开始） |
| 2026-09-06 | v1.7 | v0.4「会聊天的助手」完成：M1 需求澄清 + M2 行程生成（Node.js 零依赖实现，命令行版）。测试 5/5 通过（M1 追问/放行 2 项、M2 三城市天数校验 3 项）；新增 server/（cli 入口 + 4 个模块）与 test/（2 个测试脚本）；打 v0.4 tag。下一步 v0.5「眼见为实」（M3 地图核实 + M4 天气） |
| 2026-09-06 | v1.8 | 技术栈全面替换（用户指令）：后端 Express→FastAPI、AI 功能→LangChain（智谱 OpenAI 兼容接入）、前端规划 React→Vue 3、缓存→cachetools（本机无 Docker，Redis 列为上云项）。新增 backend/ 并保留 server/ 作对照；pytest 5/5 通过、接口冒烟与 Node 版行为一致；docs/PROJECT_BRIEF.md 升 v0.2；commit `6564200` + tag v0.4-fastapi |
| 2026-09-06 | v1.9 | 平台化说明文档 3 决策确认（用户拍板，均按推荐）：①游客可浏览、登录才能收藏/评论/发攻略/存历史；②首批 5 城（成都/杭州/西安/北京/三亚）；③图片=高德 POI 自带+无图占位。PROJECT_BRIEF.md 升 v1.0 定稿；第 8 节版本表重组为平台新序列（v0.5 数据底座 → v1.0 整装正式版）。进入效果图环节 |
| 2026-09-06 | v2.0 | 效果图四项功能变更（用户提出）：①首页热门推荐区加"查看全部景点"入口；②景点详情页收藏按钮显示本站收藏总人数 + 跳转数据来源官方网页按钮；③助手页移除 Word 行程单导出（行程单页内直接展示）、新增多方案保存与切换对比、行程单说明区、涉及景区卡片网格；④攻略列表多关键词搜索+单列大卡、详情页多图展示、编写页独立照片上传区（多张）。原型十页同步更新，PROJECT_BRIEF.md 相应章节同步 |
| 2026-09-06 | v2.1 | 效果图两项改进（用户提出，附截图）：①首页"猜你喜欢"增加分页（页码 1/2/3 + 下一页按钮，每页 12 条 = 4×3 网格铺满、共 36 条，复用景点列表页 .pager/.pg 绿色分页样式，JS 真实切页，数据限首批 5 城）；②攻略详情页排版重构为两大区块——顶部图片画廊（主图+左右箭头轮播+缩略图切换）+ 下方纯正文区，图文分离根治原排版混乱/重叠问题，含 640px 移动端响应式。PROJECT_BRIEF.md 相应章节同步 |
| 2026-09-06 | v2.2 | 景点列表页扩容（用户提出"展示的也很少"）：列表从静态 6 张 + 假分页（1/2/3/…/15 摆设）改为**每页 12 条（3 列 × 4 行）+ JS 真实切页**，3 页共 36 条覆盖首批 5 城（页 1 成都 12 条 / 页 2 杭州+西安 / 页 3 北京+三亚）；筛选默认值改为"全部城市 + 门票不限"与数据口径一致（原"成都 · 免费 86 条"叙事废弃）；混多城页卡片区域标签加城市前缀（杭州 · 西湖区 · …），单城页保持区名。PROJECT_BRIEF.md 同步 |
| 2026-09-06 | v2.3 | 参考需求清单对靶（用户提供"Python+LangGraph 智能旅游规划系统"完整功能清单，用户拍板"参考方向"）：①功能层面清单 6 项用户功能 + 5 项管理功能**基本全覆盖**（后台 8 模块超出清单）；②技术栈不照搬——Django/MySQL/DeepSeek/爬虫为"实现手段差异"，保持 FastAPI + SQLite（SQLAlchemy 预留 MySQL）+ 智谱（可切 DeepSeek）+ 高德官方接口（优于爬虫，稳定合规）；③**LangGraph 引入**为功能级增强：AI 规划编排为多节点状态图 + 流式输出，归入 v0.7；④Neo4j 知识图谱**列入远景待办**（用户确认；清单仅标题提及、正文技术列表无；不排进版本序列，触发条件=上推荐引擎/3 层以上关系推理，届时只替换 LangGraph 检索节点实现，其余架构不动，详见 SESSION_LOG 待办清单）。第 5 节技术选型表、第 8 节 v0.7 行同步；docs/SESSION_LOG.md 建档 |
| 2026-09-06 | v2.4 | **v0.5「数据底座」完成**（用户"继续"放行效果图，直接开工）：①SQLAlchemy 2.0 全套 8 表建库（users/spots/favorites/comments/guides/guide_images/guide_spots/ai_plans，对齐 BRIEF 8 后台模块），SQLite 落 backend/data/（已 gitignore），换 MySQL 只改连接串；②高德 v5 POI 官方接口采集 5 城 530 条真实景点（免费比 41.5%、有图 99.4%、坐标 100%、amap_id 零重复），免费标签按类型粗判+后台可改；③用户系统三接口（/api/auth/register、/login、/me）：PBKDF2 10 万次迭代哈希 + PyJWT HS256 token（7 天），当前用户依赖 get_current_user 供后续收藏等接口复用；④pytest 新增 6 项全过（模型关系/密码/token/POI 解析/认证流）。打 tag v0.5-data。下一步 v0.6「有门有脸」（前端工程启动） |
| 2026-09-06 | v2.5 | **GitHub 上传闭环 + v0.6 第一批完成**：①用户建仓 github.com/hzx20/Intelligent-Travel-agent-APP，28 commit + 6 tag 一次推送成功；**工作流约定生效：每完成一版必 commit+push**（已写入长期记忆）；②后端新增景点只读接口 4 个（列表筛选分页/热门Top4/猜你喜欢登录版+游客版/详情含收藏数+评论），猜你喜欢池 36 条分 3 页与原型口径对齐；③前端工程 frontend/ 手写搭建（Vue 3.5 + vue-router 4 + Vite 6，npmmirror 装依赖，dev 代理 /api→8000）；④三个页面真数据渲染：首页（轮播自动播+热门推荐+猜你喜欢翻页）、景点列表（三筛选+12 条/页分页）、详情（大图+收藏人数+高德来源跳转+评论）；⑤vite build 通过、4 接口真数据冒烟通过、pytest 15 项全过。收藏/评论写操作与后台属 v0.6 第二批 |
| 2026-09-06 | v2.6 | **v0.6 第二批完成：收藏/评论写功能 + 推送通道破障**：①后端写接口——POST /api/spots/{id}/favorite 切换式收藏（返回最新 favorited+count）、POST /{id}/comments 发表评论（1-500 字 strip 校验+可选 1-5 星，修掉纯空格入库漏洞）；详情接口带 favorited 字段（get_current_user_optional 可选登录依赖）；②前端——详情页收藏按钮（红心切换+人数实时变）+ 评论编辑器（星选+发布+新评论置顶）+ 登录弹窗全局化（store 状态任意页面唤起）；③pytest 18 项全过、uvicorn 真数据全链路冒烟 PASS（注册→收藏开→关→再收→评论→详情核验）；④**推送通道破障**：用户 FlClash 截图定位（系统代理模式）→ netstat 挖出真实端口 7890 → 固化 http.proxy + 静默凭据，确立"push 后 ls-remote 对比 SHA 终验"纪律（写入用户级长期记忆）。下一步 v0.6 收尾：个人中心（我的收藏/攻略）与管理后台 |
| 2026-09-06 | v2.7 | **v0.6「有门有脸」全部完成**：①后端——个人中心接口组（/api/me/profile 含统计、/favorites、/guides、PATCH 改昵称）+ 管理后台接口组（/api/admin/*：用户管理含停用启用/授收管理员且禁止自停、景点管理含修正采集粗判、收藏记录查看、评论审核软删、攻略管理含作者/图片/关联景点、AI 规划记录含意图提取；整组 is_admin 守卫 403）；②管理员提权脚本 backend/tools/make_admin.py（--revoke 可回收）；③前端——个人中心页（资料卡+改昵称+收藏/攻略双 tab，复用景点卡）+ 后台管理页（侧边栏 6 组模块+表格+搜索+分页+行内操作）；④pytest 21 项全过（新增 4 项：me 全流程/admin 守卫 401 403/用户停用与登录联动/评论软删）；⑤真数据冒烟 PASS（①me 接口+未提权 403 → make_admin 提权 → ②后台六模块 → ③收藏→评论→审核删除全链路 → ④数据清理）。**下一步 v0.7「图上谈兵」：LangGraph 助手（M1/M2 升级多节点编排+流式输出）+ 高德地图核实与展示** |
| 2026-09-06 | v2.8 | **v0.7 第一批（后端核心链路）+ 启动器**：①启动器 启动网站.bat（双击起双服务+自动开浏览器）；②列表页分页折叠重构（utils/pagination.js 纯函数 11 项断言全过：首尾常驻+当前±1+贴边扩展+缺1补全缺2省略；上一页/下一页首尾禁用）——**推送规程升级：push 前必须先问用户 FlClash 连接状态**（FlClash 会假死，程序探测不出，用户浏览器是唯一裁判，写入用户级长期记忆）；③**LangGraph 编排上线**（v0.7 核心）：四节点状态图 澄清→(条件边)→生成→地图核实→天气，graph.astream 逐节点流式产出；M3 地图核实服务（本地库命中→高德查证→同城替代→unverified 四级降级，虚构地点自动替换并留日志）；M4 天气服务（失败不打断）；SSE 流式接口 POST /api/plan/stream（log/token/result/done 四类事件，登录用户规划自动落库 ai_plans）；④测试 6 项全过，**测试歪打正着揪出真 bug**：图节点读 collected 用了 city 而实际字段名是 destination（核实/天气拿到空城市）——已修；⑤install langgraph（清华镜像）。前端助手页属 v0.7 第二批 |
| 2026-09-06 | v2.9 | **v0.7 第二批（前端助手页）——v0.7「图上谈兵」核心功能齐**：①后端补历史接口 GET /api/plan/history（本人列表，游客空列表提示）+ GET /api/plan/{id}（仅本人可见，他人 404 游客 401）；②前端 api 层新增 streamPlan（fetch ReadableStream 手动解析 SSE，浏览器 EventSource 不支持 POST）；③助手页 PlanView（对齐原型左对话右行程布局）：对话流打字机追问（▌光标动画）、节点过程日志实时滚动（[clarify][generate][verify][weather]）、右侧行程按天渲染（序号圆点+地址+核实徽章）、天气条、**高德 URI 导航**（全程串联+单点标记，用核实后的真实坐标，零 JS key 依赖，后续可升级内嵌地图）、历史方案下拉切换；④导航栏加"线路规划助手"；⑤测试 7 项全过（新增 history/详情权限 3 断言组）+ vite build 通过。**v0.7 剩余**：内嵌高德 JS 地图（需确认 key 平台权限）、天气 adcode 精确化 |
| 2026-09-08 | v2.10 | **v0.8「游记笔墨」全部完成 + 天气 adcode 精确化**（v0.7 收尾一半）：①后端新增 routers/guides.py（列表多关键词检索/城市筛选/最新最热排序/分页、详情含图片画廊与关联景点、发布、编辑、删除、草稿态 is_draft、浏览量 views 非作者+1）；②models 给 guides 加 views/is_draft 两列 + database.init_db 老库补列迁移（SQLite ALTER TABLE，幂等，实测真库生效）；③天气服务加城市→adcode 解析（本地字典命中首批 5 城 + 高德行政区域查询兜底 + 内存缓存 + 别名归一化"成都市"→"成都"），weather_node 无需改即可吃到精确 adcode；④前端新增三页 GuidesView（单列大图卡片流 + 多关键词搜索 + 城市/排序筛选 + 折叠分页）/ GuideDetailView（图片画廊主图+左右箭头+缩略图 + 纯正文分段 + 关联景点胶囊 + 作者编辑删除）/ GuideEditView（标题城市正文 + 图集维护首张封面 + 关联景点搜索点选 + 一键用景点图填充 + 发布/存草稿）；⑤导航加"旅游攻略"入口，个人中心攻略 tab 加"写攻略/编辑/草稿标记"；⑥测试 6 项全过 + **真库冒烟 22 项全过**（tools/smoke_v08.py，走真实 app.db 不起服务）+ vite build 通过。**剩余**：内嵌高德 JS 地图（需确认 key 平台权限）→ 之后进 v1.0 冲刺 |
| 2026-09-08 | v2.11 | **v0.7「图上谈兵」收尾：静态地图上线（用户拍板"折中方案"）**：①新增 `backend/app/routers/map.py` —— `GET /api/map/static`（后端代理高德静态地图，key 只留服务器不外泄；支持最多 20 点标记、按序连线、尺寸/缩放/编号开关、内存缓存 80 张、脏坐标容错丢弃、上游非图片响应转 502）；②`spots.SpotItem` 补 `lng`/`lat` 两个输出字段；③前端——助手页加"行程路线总览"大图（全行程点连线，点击跳高德导航）+ 每天卡片当日小图；景点详情页加"位置"静态图（点击跳高德标记页）；④测试 6 项全过（monkeypatch 替身高德，不依赖网络）+ 真库实调验证（单点 31KB / 5 点路线 41KB PNG）+ vite build 通过。**下一步：v1.0 冲刺**（全链路真实数据走查 + 边界修补 + tag v1.0） |
| 2026-09-08 | v2.12 | **v1.0 冲刺第一批（走查 + 边界 + 交付资料）**：①新增 `tools/walkthrough_v10.py` 全链路走查（游客浏览→注册登录→收藏→评论→攻略发布/草稿/检索→静态地图→后台提权与六模块→清理，**51 项全过**，含 `--with-ai` 真实调智谱生成 3 天行程 + 天气）；②新增 `tests/test_v10_edge.py` 边界测试 5 组（越界分页/不存在 id/非法筛选/输入上限/游客写保护与伪造 token）——**走查过程中揪出 1 处用例写法错误**（admin 停用接口参数走 query 串而非请求体，已修正脚本，非产品 bug）；③补齐 `README.md`（启动方式、功能清单、技术栈、目录结构、测试与走查命令、常见问题：提权/静态地图/换库/换城市）——此前 main.py 注释里提到的 README 实际并不存在 |
